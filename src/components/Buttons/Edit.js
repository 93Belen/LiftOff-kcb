import { Button } from "react-bootstrap"
import './Buttons.css';

export const Edit = () => {
    
    return (
        <Button
        id='likeButton'
        variant='outline-warning'>
            Edit
        </Button>
    )
}