import { Button } from 'react-bootstrap';
import './Buttons.css';

export const DoneAdding = () => {
    return (
        <Button
        id='doneButton'
        variant='warning'
        >Done</Button>
    )
}