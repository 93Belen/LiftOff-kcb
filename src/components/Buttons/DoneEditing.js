import { Button } from 'react-bootstrap';
import './Buttons.css';

export const DoneEditing = () => {
    return (
        <Button
        id='doneButton'
        variant='warning'
        >Done</Button>
    )
}