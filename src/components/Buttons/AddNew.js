import { Button } from 'react-bootstrap';
import './Buttons.css';

export const AddNew = () => {
    return (
        <Button
        id='addNewButton'
        variant='outline-dark'
        >Add New</Button>
    )
}

